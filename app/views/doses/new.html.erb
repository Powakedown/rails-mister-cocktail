<div class="container text-center">
  <div class="content-medium">
  <h1>Ajouter une nouvelle dose au cocktail</h1>

  <%= simple_form_for [@cocktail, @dose] do |f| %>
    <div class="form-group">
      <%= f.input :description, :label_html => { :class => 'form-control-label' }, :input_html => { :class => 'form-control' }, error: 'Le nom est obligatoire, please specify one' %>
    </div>
    <div class="form-group">
    <%= f.input :ingredient_id, :label_html => { :class => 'form-control-label' }, :input_html => { :class => 'form-control' }, label: 'Ingrédient de la dose', collection: Ingredient.all %>
    </div>
    <%= f.button :submit, class: 'btn btn-primary' %>
 <% end %>
  </div>
</div>
